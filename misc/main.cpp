#include <iostream>
#include <string>

using namespace std;

string reversed(int x);

bool isPalindrome(int x);

int main()
{
    int num = 123;
    string nums = to_string(num);
}

string reversed(int x)
{
    string stringed = to_string(x);
    for(int i = 0; i < stringed.length(); i++)
    {

    }
}

bool isPalindrome(int x)
{

}
